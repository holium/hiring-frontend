import React from "react";
import { Ship } from "./Ship";

export default {
  title: "Components/Ship",
  component: Ship,
  // add docs
  parameters: {},
};

export const Basic = () => <Ship patp="~lomder-librun" color="#ff810a" />;
